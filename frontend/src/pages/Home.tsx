import Hero from "@/components/hero";
import PlaftormStats from "@/components/plaftorm-stats";

const Home = () => {
	return (
		<>
			<Hero />
			<PlaftormStats />
		</>
	);
};

export default Home;
